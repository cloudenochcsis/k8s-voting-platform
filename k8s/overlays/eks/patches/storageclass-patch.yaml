apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: voting
spec:
  volumeClaimTemplates:
  - metadata:
      name: postgres-data
    spec:
      storageClassName: gp3
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 10Gi
